<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital_care.hospitalcare.mapper.UserRoleMapper">

    <select id="getRoleIdByUserId" resultType="com.hospital_care.hospitalcare.entity.UserRole">
        select * from user_role
        where user_id = #{userId}
    </select>
</mapper>
